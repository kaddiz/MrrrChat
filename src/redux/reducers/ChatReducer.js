
const initialState = [{
   id: 0,
   name: 'NAME',
   msg: '',
   time: 0
 }];

export default function (state = initialState, action) {
  switch (action.type) {
      return state;
    default:
      return state;
  }
}
